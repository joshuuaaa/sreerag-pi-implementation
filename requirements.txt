# requirements.txt
# Crisis Assistant – Raspberry Pi 5 production dependencies
#
# Build-time (laptop) RAG index builder also needs these packages.
#
# Pi-side install:
#   pip install -r requirements.txt

# ── LLM ──────────────────────────────────────────────────────────────────────
# For optimised builds with OpenBLAS on Pi 5:
#   CMAKE_ARGS="-DLLAMA_BLAS=ON -DLLAMA_BLAS_VENDOR=OpenBLAS" \
#   pip install llama-cpp-python==0.2.82
llama-cpp-python==0.2.82

# ── RAG & Embeddings ──────────────────────────────────────────────────────────
faiss-cpu==1.7.4
sentence-transformers==2.7.0
torch==2.2.2
transformers==4.40.2
tokenizers==0.19.1
huggingface-hub==0.23.0

# ── Configuration ─────────────────────────────────────────────────────────────
PyYAML==6.0.1

# ── Hardware (Pi only – comment out for laptop dev) ───────────────────────────
RPi.GPIO==0.7.1
RPLCD==1.3.1

# ── Audio ─────────────────────────────────────────────────────────────────────
pyaudio==0.2.14
soundfile==0.12.1
numpy==1.26.4

# ── Utilities ─────────────────────────────────────────────────────────────────
python-Levenshtein==0.25.1
requests==2.32.2
